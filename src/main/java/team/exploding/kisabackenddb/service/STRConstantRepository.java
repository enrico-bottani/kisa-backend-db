package team.exploding.kisabackenddb.service;

import org.springframework.data.jpa.repository.JpaRepository;
import team.exploding.kisabackenddb.model.STRConstant;

public interface STRConstantRepository extends JpaRepository<STRConstant, Long> {
}
